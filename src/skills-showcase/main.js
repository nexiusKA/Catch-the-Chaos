/**
 * Skills Showcase – entry point.
 *
 * Renders looping demonstrations of every skill/perk using the game engine.
 * TODO: implement per-skill canvas demos.
 */

const root = document.getElementById("skills-showcase-root");

if (root) {
  root.innerHTML = `<p style="color:#9fb5c7;text-align:center;padding:40px 0;">
    Skills showcase coming soon.
  </p>`;
}
